================================================================================
                    🚀 GET STARTED IN 5 MINUTES 🚀
================================================================================

Everything is ready. Just 2 commands to start!

================================================================================
STEP 1: OPEN TERMINAL
================================================================================

Go to your project directory:
  cd "/Users/rishiraj/Downloads/FYP React"

================================================================================
STEP 2: START BACKEND (Terminal 1)
================================================================================

Copy & paste this:

  source venv/bin/activate
  python3 -m uvicorn backend.main:app --reload

You should see:
  ✅ Model 1 (DeepLabV3 + ResNet-50) loaded successfully
  ✅ Model 2 (ResNet50) loaded successfully
  ✅ Model 3 (EfficientNet-B0) loaded successfully
  🎯 All models loaded! System ready for predictions.
  INFO:     Uvicorn running on http://127.0.0.1:8000

Keep this terminal open!

================================================================================
STEP 3: START FRONTEND (Terminal 2 - NEW TERMINAL)
================================================================================

Open a new terminal and paste this:

  cd "/Users/rishiraj/Downloads/FYP React/frontend"
  npm run dev

You should see:
  VITE v8.0.1  ready in 234 ms
  ➜  Local:   http://localhost:5173/

================================================================================
STEP 4: OPEN BROWSER
================================================================================

Go to:
  http://localhost:5173

You should see:
  ✓ Logo: "Marine Debris Detection"
  ✓ Help button (?) in top right
  ✓ Green "Connected" status indicator
  ✓ Upload area in left panel
  ✓ Feature list on right panel

================================================================================
STEP 5: UPLOAD TEST IMAGE
================================================================================

Now you can:
  1. Click upload area or drag-drop an image
  2. Supported formats: TIFF, PNG, JPG
  3. Max size: 50 MB
  4. Wait for prediction (0.3-0.5 seconds)
  5. See results:
     - Confidence bars
     - Risk assessment
     - Model comparison
     - Metadata
     - Export PDF report

================================================================================
WHAT'S NEW (UPGRADES)
================================================================================

BACKEND:
  ✅ /health endpoint - Check API status
  ✅ /info endpoint - Get model information
  ✅ Better file validation
  ✅ Detailed error messages
  ✅ Request logging with IDs
  ✅ Better startup diagnostics

FRONTEND:
  ✅ Help button (?) - Usage instructions
  ✅ Connection status - Shows if backend is online
  ✅ Error dismiss button - Easy error handling
  ✅ Request ID tracking - For debugging
  ✅ Better footer - Shows online/offline status

================================================================================
TROUBLESHOOTING
================================================================================

❌ "ModuleNotFoundError: torch"
   ✓ Make sure venv is activated:
     source venv/bin/activate

❌ "Port 8000 already in use"
   ✓ Kill the process:
     lsof -i :8000
     kill -9 <PID>

❌ "Frontend won't connect to backend"
   ✓ Check backend is running:
     curl http://127.0.0.1:8000/health

❌ "Models not found"
   ✓ Check models exist:
     ls -lh backend/models/
   ✓ Should show 3 files (~260 MB total)

For more help, see:
  - SETUP_GUIDE.md       (detailed setup)
  - QUICK_REFERENCE.md   (commands & APIs)
  - UPGRADES.md          (what changed)

================================================================================
API ENDPOINTS (Optional)
================================================================================

Test in another terminal:

Check health:
  curl http://127.0.0.1:8000/health

Get API info:
  curl http://127.0.0.1:8000/info

Get Swagger docs:
  http://127.0.0.1:8000/docs

================================================================================
IMPORTANT NOTES
================================================================================

✓ Keep both terminals open while using the app
✓ Backend must be running on port 8000
✓ Frontend runs on port 5173
✓ Models load at startup (2-3 seconds)
✓ Predictions take 0.3-0.5 seconds
✓ All files are in place, nothing to download

================================================================================
YOU'RE ALL SET! 🎯
================================================================================

Backend + Frontend are ready. Just:
  1. Terminal 1: source venv/bin/activate && python3 -m uvicorn backend.main:app --reload
  2. Terminal 2: cd frontend && npm run dev
  3. Browser: http://localhost:5173

Happy detecting! 🛰️🌊

For detailed docs, see the guides in the project folder.
